import sizes from '#styles/sizes';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  TextInput: {
    borderBottomWidth: 1,
    padding: sizes.x2,
    minWidth: '100%',
  },
});
